{
  "type": "object",
  "properties": {
    "response": {
      "type": "string",
      "description": "The main response text"
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Confidence level from 0 to 1"
    },
    "category": {
      "type": "string",
      "enum": ["positive", "negative", "neutral"],
      "description": "Response sentiment category"
    }
  },
  "required": ["response"],
  "additionalProperties": false
} 